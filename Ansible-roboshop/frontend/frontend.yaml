- name: configure frontend
  hosts: frontend
  become: yes
  tasks:
    - name: enable nginx 1.24
      ansible.buitin.command: dnf module enable nginx:1.24

    - name: install nginx
      ansible.buitin.dnf:
        name: nginx
        status: present